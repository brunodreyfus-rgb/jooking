# Jooking patch v2.7.0

Files included:
- `index.html`
- `assets/js/live-incidents.js`
- `assets/css/home-mobile-first.css`

Changes:
- Homepage incidents are sorted first by `incident_date` (newest first), then by `created_at`.
- Mobile-first homepage layout for screens up to 820px.
- More compact incident cards, clearer incident dates, horizontal category browsing, smaller map and improved mobile spacing.

Copy these files into the same paths in the repository, replacing the existing files.
